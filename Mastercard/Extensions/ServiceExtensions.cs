using Client.Common.Models;
using GlobalPay.HostedCheckouts.Mastercard.Models;
using Microsoft.VisualBasic;
using System.Net.Http.Headers;
using System.Text;

namespace GlobalPay.HostedCheckouts.Mastercard.Extensions
{
    public static class ServiceExtensions
    {
        //public static void AddHttpClients(this IServiceCollection servicesv,IConfiguration configuration)
        //{
        //    services.AddHttpClient(CardPaymentClients.MasterCardClient, c =>
        //    {
        //        c.BaseAddress = new Uri(MPGSCreds.apiBaseUrl);
        //        c.DefaultRequestHeaders.Add(Constants.EnvHeader, config.IsSandbox ? "sandbox" : "mtnghana");
        //    });
        //}

    }
}
